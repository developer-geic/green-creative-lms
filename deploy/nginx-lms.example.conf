# Example Nginx snippets for LMS FE (merge into infra/conf.d/default.conf)

# Add lms.sangtaoxanh.edu.vn to the HTTP server_name / ACME block, then:

# server {
#     listen 443 ssl;
#     http2 on;
#     server_name lms.sangtaoxanh.edu.vn;
#
#     ssl_certificate     /etc/letsencrypt/live/sangtaoxanh.edu.vn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/sangtaoxanh.edu.vn/privkey.pem;
#
#     add_header X-Robots-Tag "noindex, nofollow" always;
#
#     location / {
#         proxy_pass         http://fe-lms-sangtaoxanh:3000;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade $http_upgrade;
#         proxy_set_header   Connection 'upgrade';
#         proxy_set_header   Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

# docker-compose service sketch:
#   fe-lms-sangtaoxanh:
#     image: ${DOCKER_USER}/${LMS_FE_IMAGE_NAME}:latest
#     container_name: fe-lms-sangtaoxanh
#     restart: unless-stopped
#     env_file:
#       - /home/sangtaoxanh.edu.vn/infra/fe-lms/.env
#     networks:
#       - shared_network
